"""Email-registration OSINT via the holehe tool.

Wraps the external `holehe` binary (install via `pipx install holehe`) and
normalizes its output into the same `state/` layout as broker and account
scans. Shows up in the dashboard as an "Email exposure" card — where has
this email address been used to sign up?
"""
