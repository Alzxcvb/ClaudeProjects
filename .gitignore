# Secret / Private projects — never push to public ClaudeProjects repo
project-second-citizenship*
second-citizenship-*
market-timing-research/
R-Bourne-FVG-Trading/
Richard-Trading-Bot/
idea-ranker/

# Session state — local only
session_recovery_summary_*.md

# Personal context file — sensitive, local only
memory-master-file.md

# Session task list — ephemeral, changes every session
tasks/todo.md

# macOS Finder metadata
.DS_Store

# External repos (cloned separately, not submodules)
Qwen3-0.6B/
ai-model-router/
learning-to-prompt-claude/
github-profile/
locked-in/
netweaver-lite/
openclaw-deploy/
jarvis/
thoughtleader/
hi-im-alex/
pomodoro-calisthenics/

# Personal, machine-specific Claude Code settings (absolute paths, local hooks)
.claude/settings.local.json

# Catch-all: never commit real env files from any subfolder (defense-in-depth)
**/.env
**/.env.*
!**/.env.example
!**/.env.sample
!**/.env.template
.env.backup

# fvg-options-bot: trading strategy — keep out of public repo (nested repo today; belt-and-suspenders)
fvg-options-bot/

# Security audit docs — keep private
HIGH_RISK_PROJECT_AUDIT_CLAUDE_REVIEW.md
HIGH_RISK_PROJECT_AUDIT.md
