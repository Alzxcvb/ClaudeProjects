# Task: The service won't start

This small service simulates connecting to a database at startup. Per its README, it should print a "connected" line and exit 0. Instead it crashes.

Run it:

```bash
python3 app.py
```

Diagnose and fix whatever is wrong so the service starts successfully. `README.md` in this directory documents the intended configuration contract. Explain the root cause(s) in your report.
